# Environment for the DeepSeek‑R1‑Distill‑Qwen‑7B model
# ----------------------------------------------------
# NOTE: DeepSeek‑V2‑Chat provides a ready‑to‑use GGUF file.
# This model can be used for deep reasoning tasks (code, math, logic).
# No conversion is required – just download the GGUF via download.sh.
LLAMA_PORT=8088
LLAMA_HOST=0.0.0.0
MODEL_FILE=deepseek-v2-chat-q4_k_m.gguf
HF_REPO=DeepSeek/DeepSeek-V2-Chat-GGUF
CTX_SIZE=16384
THREADS=8
KV_CACHE=q8_0
MODEL_NAME=reasoner
ALIAS=reasoner
