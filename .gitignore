# machine-generated env (env.example 를 원본으로 둠)
server/config/env
# machine-generated model profiles (models/*.env.example 는 유지)
server/config/models/*.env
# HuggingFace 토큰 (비밀 - 절대 커밋 금지)
server/config/hf_token
# 밤새면 job/생성 결과물
server/output/

# macOS / misc
.DS_Store
*.swp

# ──────────────────────────────────────────────────────────────
# Server runtime files (logs, pid, temporary data)
# ──────────────────────────────────────────────────────────────
run/
*.log
*.pid

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Docker (실행 산출물은 커밋하지 않음 — 소스만)
*.egg-info/
.pytest_cache/

